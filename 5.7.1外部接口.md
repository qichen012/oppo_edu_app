针对教育辅助应用系统（`oppo_edu_app`）的系统设计文档，我为你撰写了**“5.7.1 外部接口”**的详细内容，涵盖了软件接口（内部微服务调用、第三方云服务）与硬件接口（GPU算力、外设输入）。你可以直接将以下内容填入你的文档中。

***

### 5.7.1 外部接口

本系统作为一个多模态的智能教育平台，在运行过程中需要与多个内部微服务、第三方云平台以及底层硬件设备进行交互。系统的外部接口主要划分为**软件接口**与**硬件接口**两大类，均遵从高内聚、低耦合的标准规范进行设计。

#### 1. 软件接口 (Software Interfaces)

软件接口主要基于 HTTP/HTTPS 协议与 RESTful 架构标准，数据交换格式统一采用 `JSON`。

**1.1 云端大语言模型 (LLM) 接口**
*   **调用对象**：智增增平台（兼容 OpenAI 规范的 API）。
*   **接口作用**：用于系统核心的认知抽象与文本生成，包括文本解析生成讲义（Handout）、提取高优洞见（Elite Ideas）、生成知识树（Knowledge Tree）以及智能伴学答疑。
*   **数据规范**：通过 `POST` 发送含有 `messages` (包含系统提示词与用户上下文) 的 JSON，配置 `response_format={"type": "json_object"}` 强制返回结构化数据。
*   **安全机制**：通过环境变量注入 `ZHIZENGZENG_API_KEY` 进行 Bearer Token 鉴权。

**1.2 视觉与文生图模型 (VLM & T2I) 接口**
*   **调用对象**：智增增 Google v1beta (Gemini) 接口。
*   **接口作用**：
    1. 前端截图多模态要素提取（OCR、版面分析）。
    2. 基于抽象的 Meta Ideas 自动生成知识图卡配图。
*   **数据规范**：图像以 `Base64` 编码封装在 JSON 的 `parts/inlineData` 中上传，返回 `image/png` 等格式的 Base64 数据流（经本地 Pillow 裁剪后保存）。

**1.3 本地独立语音识别 (FunASR) 接口**
*   **调用对象**：部署于 Windows 端的 GPU 语音微服务。
*   **接口作用**：接收主系统转发的课堂/会议的长音频文件，进行说话人分离、静音检测和高精度中文转写。
*   **数据规范**：采用 `HTTP POST multipart/form-data` 以分片形式传输 `.wav` 等音频文件至 `/transcribe` 端点，返回带有 `speaker_id`、`start_timestamp` 且按句切分的结构化文本 JSON。

**1.4 持久化与向量记忆库 (Mem0) 接口**
*   **调用对象**：开源 Mem0 记忆管理层。
*   **接口作用**：存储、嵌入（Embedding）及检索用户长期的学习偏好、历史会话中出现的关键薄弱点。
*   **数据规范**：通过系统的 `mem0_manager` 包装库调用，按 `user_id` 和 `agent_id` 划分 Namespace 存取向量记录。

**1.5 关系型数据同步接口 (Learning_DB)**
*   **调用对象**：独立运行的 Learning_DB 后端服务平台（端口8000）。
*   **接口作用**：承担后台高价值数据的结构化入库（Elite IdeasCards、Cases、Daily Briefings 等）。
*   **数据规范**：遵循 RESTful 规范（如 `POST /api/v1/elite-idea-cards`），传入严格对齐数据库 ORM 模型字段的 JSON。依靠主程序的 `persist_index.json` Hash 校验作为拦截，保证同一结果只写入一次（幂等性）。

**1.6 搜索引擎与外部数据源接口**
*   **调用对象**：DuckDuckGo (DDGS) 与外部 RSS Feed。
*   **接口作用**：为系统的 `Deep Search`（深度搜索）模块提供实时的外部互联网知识验证与相关前沿资料扩展。
*   **数据规范**：标准 HTTPS Request 抓取外网数据体，并通过 BeautifulSoup 进行 HTML 标签清洗。

---

#### 2. 硬件接口 (Hardware Interfaces)

系统在数据处理密集型任务与多模态信息采集过程中，需要依赖计算机的底层物理硬件环境支撑，主要包含以下硬件接口及驱动协议：

**2.1 GPU 硬件加速及并发计算接口**
*   **接口描述**：NVIDIA CUDA (Compute Unified Device Architecture) 及 cuDNN 驱动接口。
*   **协同机制**：语音处理微服务层（FunASR）在载入 `paraformer-zh`、`fsmn-vad`、`cam++` 等声学模型时，直接通过 PyTorch 框架调用 `device="cuda"` 接口拉起 GPU。显卡显存(VRAM)及流处理器被用来大规模并行处理批次（`batch_size_s`）的音频矩阵张量，解决 CPU 解码耗时过长、无法实时响应的问题。

**2.2 存储子系统与磁盘 I/O 接口**
*   **接口描述**：操作系统（Windows/macOS）底层的 POSIX File System 接口。
*   **协同机制**：系统生成的大量 Markdown 文件、JSON 数据以及图片（Image Bytes），需要高频次调用磁盘 I/O 进行落盘（持久化于 data 目录下）。系统利用 Python 的 `os` 与 `json` 标准库经由操作系统对固态硬盘（SSD）发起高速读写请求。

**2.3 外设感知与输入接口（间接调用）**
*   **接口描述**：麦克风（音频采集输入源）和摄像头（图像采集输入源）接口。
*   **协同机制**：作为系统的数据输入端，各类 Web/App 前端应用需调用系统底层硬件 API 提供录音环境和拍摄环境。所获取的原始模拟信号经过硬件声卡/ISP芯片采样压缩后，转换成系统服务端可识别的数字信号介质（如数字 WAV 音频流，JPEG 图像字节），再进行后续处理。
### 5.7.2 内部接口

教育辅助应用系统（`oppo_edu_app`）的内部模块解耦良好，各核心“技能（Skill）”模块与主服务端、数据库端之间通过**内部函数调用（Function API）**与**内部 RESTful 接口**相结合的方式进行通信。这种内部接口设计既保证了单体内部处理的极速性，又预留了未来横向扩展为微服务架构的灵活性。

#### 1. 软件内模块函数接口 (Internal Function-based Interfaces)

大部分核心大模型业务逻辑模块置于系统 `skill/` 包下，由主服务（`run/server.py`）通过 `import` 方式进行函数阻塞调用或交给后台异步线程执行。

**1.1 PDF 处理函数接口**
*   **接口签名**：`process_pdf_to_handout(pdf_path, filename=None, max_pages=30) -> dict`
*   **调用者**：主路由网关。
*   **被调用者**：`lecture_handout_generator.module` 讲义生成引擎。
*   **接口规范**：参数传入已暂存本地的 PDF 绝对路径，系统同步执行文本提取、截断、LLM讲义生成动作，组装为指定格式的 Dict。返回值包含结构化数据 payload 与对应的 Markdown 字符串。

**1.2 知识树生成函数接口**
*   **接口签名**：`process_bytes_to_knowledge_tree(pdf_bytes, filename="") -> dict`
*   **调用者**：主路由网关（通常裹载于 `fastapi.BackgroundTasks` 中执行）。
*   **被调用者**：`knowledge_tree_generator.module` 知识树引擎。
*   **接口规范**：为跳过重复的磁盘读取阶段，参数接受由前端上传直接转换好的 `bytes` 流；输出返回校验符合 Pydantic 模型（含父子层级关系，以及 `relatedNodeId` 指针字段）的大型 JSON 树。

**1.3 高级洞见（Elite Ideas）抽取接口**
*   **接口签名**：`process_handout_to_elite_ideas(source_file=None, handout_filename=None, ...) -> dict`
*   **调用者**：讲义生成模块执行完毕后，内部流转调用。
*   **被调用者**：`elite_ideas_extractor.module` 洞见提取引擎。
*   **接口规范**：参数接受**已生成的**讲义文件标识（解耦了再次读取大型 PDF 的限制）；返回值输出严格对齐底层表结构（`elite_idea_cards` 等字段设计）的 Payload 字典，并自动向内部 T2I 引擎发起配图请求。

**1.4 截图关联与信息全量提取函数接口**
*   **接口签名**：
    *   `extract_all_info_from_screenshot_bytes(image_bytes: bytes) -> dict`
    *   `analyze_screenshot_brief_association(extracted_info: dict, daily_brief: dict) -> dict`
*   **接口规范**：图片打解包流转接口。第一层全量提取，返回布局、表格、文字实体的复合 JSON。第二层根据上述结果与当前的简报文本，进行知识耦合推理打分，输出最终相关性结论与未匹配点（Unmatched points）。

#### 2. 系统内部 RESTful 微服务接口 (Internal RESTful Interfaces)

为了平衡并发负荷以及隔离底层数据，系统内部分服务通过监听 `localhost` 不同端口的形式构建本地微服务通信网。

**2.1 异步落库微服务间接口**
*   **调用者**：生成大模型产物后的 `run/server.py` 主程序（端口8001）。
*   **被调用者**：`Learning_DB` 数据持久化后端微服务（端口8000）。
*   **通信协议**：基于 `requests.Session` 发起本地 HTTP/1.1 请求（硬编码禁用代理 `trust_env=False` 防阻断）。
*   **关键接口**：
    *   `POST http://localhost:8000/api/v1/elite-idea-cards/`
    *   `POST http://localhost:8000/api/v1/elite-idea-cases/`
*   **接口规范**：主服务将清洗截断好、格式完全对齐 Pydantic 校验类的数据以 JSON Body 上送。遵循“顺序关联插入法则”，即优先拿到 Card API 返回的 `id`（自增主键），再将此 `id` 赋值作为 Case API 的外键 `meta_id` 进行关联性录入。

**2.2 自动转录微服务转发接口**
*   **调用者**：主路由网关的 `/upload_audio` 接口。
*   **被调用者**：调度模块 `skill/meeting_transcriber.py`（内部封装对转录服务器发起请求）。
*   **通信协议**：HTTP POST `multipart/form-data`。
*   **接口规范**：通过配置文件提取转录微服务基址，将用户请求中的 Wav 收流打包重定向至远端/本机的 `http://<Transcribe_IP>:<Port>/transcribe`。返回具有高精结构数组的 JSON，本接口承担透明的内部数据代理(Agent)作用。

**2.3 本地化系统状态及幂等同步标记**
*   **接口表现**：不通过函数直接互调，通过对操作系统的同一文件读写锁（OS File Lock）实现数据一致同步。
*   **实现机制**：系统生成结果后，以固定逻辑针对入参计算 Hash（如 `sha1(json.dumps(payload))`）。各个异步线程、路由处理端同时读写 `data/.../learning_db_persist_index.json` 文件以确认数据状态。此文件模拟了一个内部状态锁机，充当了代码线程与数据库插入过程间的隔离保障接口。
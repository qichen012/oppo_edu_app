### 5.8 异常处理设计

#### 5.8.1 错误异常输出消息

系统作为一个多模态、依赖大模型且高度异步化的复杂平台，在遇到各类内部错误或外部依赖失联时，需要有一套结构化、易于排查的异常输出机制。

异常输出消息采用统一的 JSON 格式结构返回给调用方，至少包含以下内容：错误代码（`error_code`）、错误类型（`error_type`）、错误详情（`message`）以及发生阶段追踪（`stage`）。对于内部日志，还会记录异常堆栈跟踪。

以下为系统主要出现的错误异常及输出消息规约：

**1. 多模态文件输入与解析类异常**
*   **触发场景**：用户上传了格式不正确、损坏的文件，或者 PDF 全为图片无法提取文字（扫描件且无 OCR 处理）。
*   **典型输出**：
    ```json
    {
      "error_code": 4001,
      "error_type": "DataExtractionError",
      "message": "无法从 PDF 中提取有效文字或提取内容过短，疑似扫描件脱机。",
      "stage": "extract_pdf_text_and_headings",
      "action_suggested": "请确认传入文档包含有效文本层，或使用 OCR 辅助工具。"
    }
    ```

**2. LLM / 大模型调用相关异常**
*   **触发场景**：包括智增增、Gemini、OpenAI API 响应超时、Token 上下文超出限制、或网络被墙/断开。
*   **典型输出**：
    ```json
    {
      "error_code": 5002,
      "error_type": "LLMGatewayTimeout / LLMContextWindowExceeded",
      "message": "AI 服务响应超时或当前请求 Token (142,000) 超过模型最大允许长。",
      "stage": "generate_handout_json / extract_elite_ideas",
      "action_suggested": "尝试缩小上传文件的大小或缩减处理篇幅。"
    }
    ```

**3. 结构化 JSON 解析与兜底异常**
*   **触发场景**：大模型没有乖乖按照 Prompt 的约束输出纯 JSON，或者包含非法的转义符，导致 Python `json.loads()` 或 Pydantic 校验崩溃。
*   **典型输出**：
    ```json
    {
      "error_code": 5003,
      "error_type": "JSONDecodeError / PydanticValidationError",
      "message": "LLM 返回的输出无法解析为结构化字典，缺少必要的 'title' 字段。",
      "stage": "build_db_aligned_payload",
      "action_suggested": "已触发系统自修复重试，若持续失败请检查输入的复杂程度。"
    }
    ```

**4. 数据库同步与落库异常**
*   **触发场景**：后台异步将生成的 JSON 回写入 `Learning_DB` 数据表时，发生外键约束不匹配（如 MySQL 3780 错误）、目标端口失去响应等。
*   **逻辑说明**：这类错误属于“旁路异常”，**不会**阻断向客户端返回的主要 HTTP 200/204 响应。
*   **内部日志输出**：
    ```text
    ERROR: [Idempotent Sync] 落库 Elite Ideas 失败，HTTP 500 / Connection Refused.
    Payload Hash: a91b2...
    Reason: Cannot add or update a child row: a foreign key constraint fails.
    Action: 该任务将不标记为 done，下一次读取文件时将重新发起尝试。
    ```

**5. 扬声器/视觉 GPU 离线异常 (FunASR/本地微服务)**
*   **触发场景**：向 `http://localhost:8000/transcribe` 投递语音或尝试调用本地模型加速时由于显存爆炸(OOM)或微服务未开启导致失败。
*   **典型输出**：
    ```json
    {
      "error_code": 5003,
      "error_type": "LocalMicroserviceOffline",
      "message": "转录服务器(FunASR)无法连接或遇到 CUDA Out of Memory。",
      "stage": "transcribe_audio_file"
    }
    ```
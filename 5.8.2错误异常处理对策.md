### 5.8.2 错误异常处理对策

本项目作为一个多模态教育辅助平台，重度依赖外部大模型（LLM/T2I），且系统内部整合了本地语音识别（FunASR）和文档结构化解析（PyMuPDF），具有重 I/O 流转与高算力消耗的特征。在面临局部模块失效、网络波动或高并发大文件处理压力时，为避免雪崩效应，系统针对各类错误与异常设计了多级熔断与兜底对策。具体从以下四个维度进行全链路保障：

#### 1. 核心链路资源后备机制（Fallback）
当首选方案（网络请求、主端模型或特定算法）因为网络隔离或服务宕机而报错时，系统将无缝切换备用路径以保障主流程不断链：
*   **大模型网关与通道替换（LLM API Fallback）：**
    *   在网关层捕获到 `LLMGatewayTimeout` 或持续遇到 `JSONDecodeError` （输出格式发散）时，主控模型（如经游智增增平台代理的 GPT-4o）的请求池会将该节点标记为亚健康，系统 `skill/config.py` 路由器会平滑回退至后备模型节点或本地轻量级开源大模型。
    *   若多次请求无法获取严格的 JSON Object，系统自动切换 Prompt 模板至“强函数调用（Function Calling）”约束模式发起重试。
*   **多模态解析能力降维（Parser Fallback）：**
    *   针对 `DataExtractionError` 异常，如在 `screenshot_analyzer.py` 处理复杂图文混排、截屏公式提取失败时，系统将退化并后备使用最基础的纯文本提取模式（如回退至 `PyMuPDF` 的普通 `get_text()` 方法），放弃复杂的版面还原，保全核心信息构建知识树。
    *   若本地 GPU 驱动的 `FunASR` 语音识别服务不可用或无法加载模型，临时降级调用无依赖约束的通用云端 ASR 接口，或及时挂起并通知前端“录音分析受限”。
*   **双写存储与持久化后备（Storage Fallback）：**
    *   当本地 MySQL（`Learning_DB`）爆发 `DBSyncFailure` 时，系统将启用本地文件系统接管机制，所有提取到的精英想法、知识树节点状态立刻以 JSON 形式追加落盘至 `data/processed_notes/`，将 DB 事务转作本地日志缓存。等待数据库自愈后，数据引擎再读取重放恢复。

#### 2. 算力与性能动态降级（Performance Degradation）
当系统资源（CPU 利用率、GPU 显存压力、网关并发）达到设定的预警水线时，主动剥离或挂起高算力及非核心特性，“以局部限流换全局在线”：
*   **重负载微服务剥离暂断：**
    *   精英想法提取流水线中的“文本转图像”（Text-to-Image / Gemini Vision）生成耗时长、API 并发限制严。触发高负载预警后，该功能服务被主动阻断拦截，系统降级为**仅返回核心文本和想法节点**，不在后台排队生图阻塞队列。
*   **计算阻塞转异步延期调度：**
    *   当出现群批量并发大文件（如多本长篇教材）上传，使得 `FastAPI BackgroundTasks` 或队列堆积时，Mem0 高维向量知识插入与结构化节点生成的“实时性”将被降级。系统将该类耗时较长的 IO 及索引构建延期至“批处理低谷时间窗口（Nightly Batch/Deferred Shift）”进行。
*   **API 弹性并发与问答优先级限流（Rate Limiting）：**
    *   在微服务降级期间，非核心的请求将被直接阻断并快速响应 HTTP 429。以保证用户客户端最核心的轻量化 Chat 问答、本地数据库基础条目读取的网络连通不受侵占。

#### 3. 容灾恢复及自动化重启（Recovery & Restart）
针对 OOM 溢出卡死（如上传超限长文本且未切片打爆显存）或内核僵死产生的 `LocalMicroserviceOffline` 异常，部署守护进程进行自治恢复：
*   **进程守护自拉起（Process Restart）：**
    *   依托 Docker Compose 的健康检查与 `restart: always` 策略，`Learning_DB` 节点与 `FunASR` 推理服务等一旦崩溃将迅速被重新分配内存并拉起容器。
    *   FastAPI 后端主控配合多 worker 热备管理机制（Uvicorn/Gunicorn），若子 worker 陷入僵死，调度器将在秒级监测并清理僵尸实例、重启新 worker 接管请求流。
*   **断点状态恢复与幂等对齐（Idempotent State Recovery）：**
    *   基于 `persist_index.json` 和数据哈希散列值的幂等设计，当程序因断电发生强制崩溃后再启动时，系统冷启动加载阶段会进行一致性对齐。通过扫描对比目前 DB 记录与 `data/` 下的文件列表差异，将漏处理的幽灵脱机任务再次重新掷入消息队列补偿闭环。
*   **死锁防循环与死信隔离机制（DLQ Recovery）：**
    *   对因“毒文件”（如损坏的音频流或彻底加密无法脱壳的 PDF）导致的持续触发重试死锁循环，设置最大重试次数阈值（如3次）。超限后直接阻断该链路，并把污染文件移入系统异常“死信目录”（Dead Letter File），抛出 `permanent_failure` 交由管理员干预，防止其再次消耗重启动资源。

#### 4. 前端交互体验降级兜底（UX Fallback）
当后端采用限流、降级或延期处理时，前端界面配合进行无缝兜底提示，避免用户产生系统损坏的错觉：
*   **异步骨架屏与事件轮询（Loading Placeholder）：** 当每日简报或知识树推迟到异步窗口分析时，后台只响应 202 并下发任务标识（TicketID）。前端借助骨架屏动画进行安抚，并通过 TicketID 轮询处理状态返回“排队中/分析中”。
*   **视觉缺省占位（Image Fallback）：** 当精英想法由于网络或降级使得 T2I 图像未就绪时，服务返回缺省静态配置，前端渲染系统预置的占位插画资源，保持 UI 结构的完好统一。
